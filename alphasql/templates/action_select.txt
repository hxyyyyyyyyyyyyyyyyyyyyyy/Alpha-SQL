**Task Description:**
You are an expert SQL query generator. Given the current state of SQL query generation process, please analyze the current state and select the most appropriate next action.

**Consider:**
1. Have we done enough preparation to generate SQL? If done, feel free to generate SQL directly, If not, please consider what information is still missing and choose actions that help gather more information.
2. What is the logical next step in the SQL generation process?
3. If SQL has been generated, check its compilation status to decide next steps. If SQL compilation FAILED, you have to choose "SQL Revision".

**Rules:**
1. You MUST choose exactly one action from Available Actions.
2. Copy the action name EXACTLY from Available Actions (case-sensitive, no rewrite).
3. Do NOT output any action that is not in Available Actions.
4. Do NOT choose an already executed action.

Here is an example:

Database Tables:
Tables: customers, orders

Current State:
Question: For orders placed in 2023, how many have total_amount greater than 500 and status is missing?
Hint: placed in 2023 refers to order_date >= '2023-01-01' and order_date < '2024-01-01'; status is missing refers to status is null or status = ''
- Rephrased Question: Count orders in 2023 with total_amount > 500 and missing status (NULL or empty string).
- Generated SQL: SELECT COUNT(*) FROM customers AS t1 JOIN orders AS t2 ON t1.customer_id = t2.customer_id WHERE t2.order_date >= '2023-01-01' AND t2.order_date < '2024-01-01' AND t2.total_amount > 500 AND (t2.status IS NULL OR t2.status = '')
- SQL Compilation: FAILED
  Error: no such column: t1.customer_id

Available Actions:
1. End: Finish the current reasoning path
2. SQL Revision: Revise the previously generated SQL query based on execution results

Answer:
```xml
<think>
The SQL has already been generated but failed compilation with error 'no such column: t1.customer_id', which indicates the current query is invalid and must be corrected. Given the current state and available actions, the most appropriate next action is 'SQL Revision'.
</think>
<answer>
SQL Revision
</answer>
```

**************************
Now, answer the real question, and you need to follow the answer style of the above examples.

Database Tables:
{schema_summary}

Current State:
{context}

Available Actions:
{action_options_str}

Answer:

Only output xml format (starting with ```xml and ending with ```) as your response.
